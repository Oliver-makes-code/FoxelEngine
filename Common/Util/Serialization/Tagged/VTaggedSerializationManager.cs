namespace Common.Util.Serialization.Tagged;

public static class VTaggedSerializationManager {
    private static readonly Dictionary<Type, VTaggedTypeSpec> TypeSpecs = new Dictionary<Type, VTaggedTypeSpec>();
    
    
    
}
